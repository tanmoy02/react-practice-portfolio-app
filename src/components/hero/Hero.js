import Slider from "../slider/Slider";

function Hero() {
  return (
    <section id="colorlib-hero" className="js-fullheight" data-section="home">
      <Slider></Slider>
    </section>
  );
}

export default Hero;
